import { FirstItem, LoginHeaderContainer, SecondItem } from "./style";

const LoginHeader = () => {
  return (
    <LoginHeaderContainer>
      <FirstItem />
      <SecondItem />
    </LoginHeaderContainer>
  );
};

export default LoginHeader;
